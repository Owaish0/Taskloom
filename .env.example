REDIS_URL=redis://localhost:6379/0
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=http://localhost:5173
WORKER_POLL_TIMEOUT=5

# Phase 5: required for the "summarize" task type. Get a free key from
# https://aistudio.google.com/apikey (Google AI Studio, Gemini Developer
# API — no billing setup needed for the free tier) — never commit a real
# key, this file is a template only (the real .env is gitignored).
GOOGLE_API_KEY=
SUMMARIZE_MODEL=gemini-flash-latest
